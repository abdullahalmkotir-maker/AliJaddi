# إصدارات متجر علي جدّي (بيتا)

| الملف | الوصف |
|-------|--------|
| `AliJaddiStore-0.1.0-beta.zip` | حزمة تثبيت سطح المكتب (نمط Ali12). فك الضغط ثم `Install-StoreAliJaddi.ps1`. |

لإعادة البناء من المستودع:

```bash
cd "Store AliJaddi"
python run_store12.py export-zip
```

يُحدَّث أيضاً ملف في مجلد **التنزيلات** لديك.
