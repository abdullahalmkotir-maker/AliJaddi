name: alijaddi_flutter
description: عميل Flutter (Dart) لمنصّة علي جدّي — اتصال بالبوابة التجريبية FastAPI أو خدمات أخرى.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
